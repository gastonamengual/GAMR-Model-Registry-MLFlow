apiVersion: v1
kind: Service
metadata:
  name: flower-model-service
spec:
  type: ClusterIP
  selector:
    app: flower-model-service
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5001
